
const initialState = {
    countries: [],
    languages: []
};

const data = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default data;